version: 1
update_configs:
  - package_manager: javascript
    directory: /
    update_schedule: live
    allowed_updates:
      - match:
          dependency_name: svelte
      - match:
          dependency_name: url-pattern